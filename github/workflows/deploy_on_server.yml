name: build and push and deploy

on:
  push:
    branches:
      - qa

permissions:
  contest: read
  packages: write

jobs:
  build-and-deploy:
    name: build & push
    runs-on:
      - self-hosted


    environment: ${{ github.ref_name }}

    steps:
      - uses: actions/checkout@v3

      - name: Wyświetl napis
        run: echo "HELLOO"
